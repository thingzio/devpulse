services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: devpulse
      POSTGRES_USER: devpulse
      POSTGRES_PASSWORD: devpulse
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "devpulse"]
      interval: 2s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
